# name: Build & Deploy Frontend

# on:
#   push:
#     branches:
#       - dev
#       - staging
#       - main
#   pull_request:
#     branches:
#       - staging
#       - main

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ${{ github.repository }}
#   DEPLOY_PATH: /workspace/shared/public/mvppgf/frontend-uc-prn-prd
#   COMPOSE_FILE: docker-compose.yml
#   SERVICE_NAME: frontend
#   CONTAINER_NAME: frontend-mvp
#   HEALTHCHECK_URL: http://localhost:3001
#   VITE_API_URL: http://backend:3000

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build and push Docker image (sha)
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           tags: |
#             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
#           build-args: |
#             VITE_API_URL=${{ env.VITE_API_URL }}

#       - name: Build and push Docker image (latest)
#         if: github.ref_name == 'main'
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           tags: |
#             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
#           build-args: |
#             VITE_API_URL=${{ env.VITE_API_URL }}

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     if: github.ref_type == 'branch' && (github.ref_name == 'staging' || github.ref_name == 'main')
#     steps:
#       - name: Set image tag
#         id: image
#         run: |
#           echo "image_tag=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}" >> $GITHUB_OUTPUT

#       - name: Deploy frontend via SSH
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USER }}
#           port: ${{ secrets.VPS_PORT }}
#           key: ${{ secrets.VPS_SSH_KEY }}
#           script: |
#             export REGISTRY=${{ env.REGISTRY }}
#             export REGISTRY_USER=${{ secrets.GHCR_USER }}
#             export REGISTRY_TOKEN=${{ secrets.GHCR_TOKEN }}
#             export APP_DIR=${{ env.DEPLOY_PATH }}
#             export COMPOSE_FILE=${{ env.COMPOSE_FILE }}
#             export SERVICE_NAME=${{ env.SERVICE_NAME }}
#             export CONTAINER_NAME=${{ env.CONTAINER_NAME }}
#             export HEALTHCHECK_URL=${{ env.HEALTHCHECK_URL }}
#             export FRONTEND_IMAGE=${{ steps.image.outputs.image_tag }}
#             bash ${{ env.DEPLOY_PATH }}/scripts/deploy_frontend.sh
